import React from 'react';
import TodoList from './TodoList';

const Todos = () => {
    return (
        <div>
            <h1>일정관리 만들기UI</h1>
            <TodoList />
        </div>
    );
};

export default Todos;